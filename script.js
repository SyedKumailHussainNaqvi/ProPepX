const progressBar=document.querySelector('.progress-bar');const menuToggle=document.querySelector('.menu-toggle');const navLinks=document.querySelector('.nav-links');const navAnchors=document.querySelectorAll('.nav-links a');const revealElements=document.querySelectorAll('.reveal');const counters=document.querySelectorAll('[data-count]');const modal=document.getElementById('videoModal');const modalVideo=modal?modal.querySelector('video'):null;function updateProgress(){const s=window.scrollY;const h=document.documentElement.scrollHeight-window.innerHeight;const p=h>0?s/h*100:0;if(progressBar)progressBar.style.width=`${p}%`}window.addEventListener('scroll',updateProgress,{passive:true});updateProgress();if(menuToggle&&navLinks){menuToggle.addEventListener('click',()=>{const o=navLinks.classList.toggle('open');menuToggle.setAttribute('aria-expanded',String(o))})}navAnchors.forEach(a=>{a.addEventListener('click',()=>{navLinks.classList.remove('open');menuToggle?.setAttribute('aria-expanded','false')})});const revealObserver=new IntersectionObserver(e=>{e.forEach(x=>{if(x.isIntersecting){x.target.classList.add('visible');revealObserver.unobserve(x.target)}})},{threshold:.12});revealElements.forEach(el=>revealObserver.observe(el));function animateCounter(el){if(el.dataset.done)return;el.dataset.done='true';const t=Number(el.dataset.count);const d=1200;const start=performance.now();function step(now){const p=Math.min((now-start)/d,1);const e=1-Math.pow(1-p,3);el.textContent=(t*e).toFixed(3);if(p<1)requestAnimationFrame(step);else el.textContent=t.toFixed(3)}requestAnimationFrame(step)}const counterObserver=new IntersectionObserver(e=>{e.forEach(x=>{if(x.isIntersecting){animateCounter(x.target);counterObserver.unobserve(x.target)}})},{threshold:.45});counters.forEach(c=>counterObserver.observe(c));const sections=[...document.querySelectorAll('header[id], section[id]')];const activeObserver=new IntersectionObserver(e=>{e.forEach(x=>{if(x.isIntersecting){navAnchors.forEach(a=>a.classList.toggle('active',a.getAttribute('href')===`#${x.target.id}`))}})},{rootMargin:'-42% 0px -50% 0px',threshold:0});sections.forEach(s=>activeObserver.observe(s));document.querySelectorAll('.open-video').forEach(b=>{b.addEventListener('click',()=>{if(!modal)return;modal.classList.add('active');modal.setAttribute('aria-hidden','false');if(modalVideo){modalVideo.currentTime=0;modalVideo.play().catch(()=>{})}})});document.querySelectorAll('[data-close-modal]').forEach(b=>{b.addEventListener('click',()=>{if(!modal)return;modal.classList.remove('active');modal.setAttribute('aria-hidden','true');if(modalVideo)modalVideo.pause()})});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&modal?.classList.contains('active')){modal.classList.remove('active');modal.setAttribute('aria-hidden','true');if(modalVideo)modalVideo.pause()}});